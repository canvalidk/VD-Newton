# Superseded Design 2.1 Material

This folder holds old working artifacts that have been moved out of the active
root but preserved for provenance.

No files are currently stored here.

Only move files into this folder when they are explicitly superseded by a newer
working artifact. Do not move active output drafts here merely because their
filename contains "draft".
